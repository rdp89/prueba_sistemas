Para generar el contenedor docker desde el Dockerfile tendremos que usar el comando $docker build -t nginx:final . (Estoy situado en la misma carpeta que el Dockerfile)
Para ejecutar el contenedor y este escuche desde el puerto exterior 8080, tendremos que poner el comando $docker run -d -p 8080:80 nginx:final
